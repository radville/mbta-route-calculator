<h1>Subway Routes</h1>

<ul>
    <% @routes.each do |route| %>
        <li><%= route %></li>
    <% end %>
</ul>

<h2>Subway Route with the most stops</h2>
<%= @most.name %> - <%= @most.stops.size %> stops